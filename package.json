{
  "name": "insurance-advisor-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Bot de WhatsApp para cotizacion y consulta de polizas de seguros (La Caja). Motor hibrido: menus + LLM.",
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "chat": "tsx src/main/chat.ts",
    "funnel": "tsx src/main/funnel.ts",
    "eval:emotions": "tsx src/main/eval-emotions.ts",
    "eval:faq": "tsx src/main/eval-faq.ts",
    "dev": "tsx watch src/main/server.ts",
    "start": "node dist/main/server.js",
    "build": "tsc && node scripts/copy-assets.mjs",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf .test-tmp && LACAJA_DATA_DIR=.test-tmp ANTHROPIC_API_KEY= tsx --test src/domain/*.test.ts src/domain/conversation/*.test.ts src/domain/quoting/*.test.ts src/domain/analytics/*.test.ts src/domain/faq/*.test.ts src/application/*.test.ts src/infrastructure/persistence/*.test.ts src/infrastructure/messaging/*.test.ts src/infrastructure/faq/*.test.ts src/config/*.test.ts src/main/*.test.ts",
    "check:arch": "node scripts/check-arch.mjs",
    "format": "biome format --write src scripts",
    "lint": "biome check src scripts",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,mjs}": "biome check --write --no-errors-on-unmatched"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "pg": "^8.22.0",
    "redis": "^4.7.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@commitlint/cli": "^21.2.1",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.20.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
